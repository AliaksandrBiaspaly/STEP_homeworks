
import pygame
import sys

pygame.init()
screen = pygame.display.set_mode((1200, 800))

while true:
    for event in pygame.event.get():
        if nevent.type == pygame.QUIT:
            pygame.quit()
            sys.exit()

